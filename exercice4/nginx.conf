
user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /run/nginx.pid;


events {
    worker_connections  1024;
}


stream {

    upstream adminweb {
        server admin:8080;
    }

    server {
        listen 90;
        proxy_pass adminweb;
    }

}
